{
  "name": "rajdilawar",
  "version": "1.0.0",
  "description": "Cypress POM with docker",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --headless",
    "docker:test": "docker build -t cypresspom-docker . && docker run -it --rm cypresspom-docker",
    "test-allure": "cypress run --env allure=true"
  },
  "author": "Rjdilawar Singh Chandehok",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "cypress": "^12.14.0",
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1",
    "typescript": "^4.5.4"
  }
}
